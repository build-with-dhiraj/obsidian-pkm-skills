---
title: Learning Python basics
entities:
  - python3
  - py
topics:
  - PKM
---

# Learning Python basics

This note demonstrates the resolver. After `obsidian-orphan-killer resolve
--vault .`, the `entities:` list will become `[[python]]` (from the `python3`
and `py` aliases on the entity hub) and `topics:` will become
`[[knowledge-management]]` (from the `PKM` alias on the concept hub).

A re-run is a no-op because of the `raw_meta.resolved_at` idempotency stamp.
