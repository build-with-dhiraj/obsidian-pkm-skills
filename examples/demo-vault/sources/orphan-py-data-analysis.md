---
title: Python data analysis snippets
---

# Python data analysis snippets

This is an orphan with no frontmatter entities/topics. Its body mentions
python, pandas, numpy. After `anchor`, it should attach to a relevant hub
if and only if one exists at a high enough cosine. In the default
concepts-only mode, it should NOT anchor to `[[python]]` (an entity hub)
because the safer default is to never link a generic how-to to a brand hub
without explicit opt-in.

```python
import pandas as pd
df = pd.read_csv("file.csv")
df.head()
```
